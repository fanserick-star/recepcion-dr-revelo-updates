name: Publicar v4.4.19 prueba WhatsApp completa

on:
  push:
    branches: [main]
    paths:
      - 'build/v4419_whatsapp_test_retention/**'
      - '.github/workflows/publish-v4419-whatsapp-test-retention.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-v4419-whatsapp-test-retention
  cancel-in-progress: false

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Construir v4.4.19 desde v4.4.18
        shell: pwsh
        run: |
          git pull --rebase origin main
          python build/v4419_whatsapp_test_retention/build_release.py
          python -m py_compile updates/v4_4_19_whatsapp_test_retention/app.py
          node --check updates/v4_4_19_whatsapp_test_retention/static/app.js

      - name: Validar alcance y seguridad
        shell: pwsh
        run: |
          @'
          from pathlib import Path
          import json
          root=Path('updates/v4_4_19_whatsapp_test_retention')
          app=(root/'app.py').read_text(encoding='utf-8-sig')
          js=(root/'static/app.js').read_text(encoding='utf-8-sig')
          html=(root/'static/index.html').read_text(encoding='utf-8-sig')
          channel=json.loads(Path('build/v4419_whatsapp_test_retention/candidate_latest.json').read_text(encoding='utf-8-sig'))
          assert 'APP_VERSION = "4.4.19"' in app
          assert 'Prueba técnica cerrada en pantalla' in app
          assert 'elimina automáticamente al cumplir 2 horas' in app
          assert 'source_hash LIKE :prefix' in app
          assert 'data-section="whatsappRespuestas"' in html
          assert 'resolveWhatsappResponse' in js
          assert channel['version']=='4.4.19'
          assert all('/main/' in f['url'] for f in channel['files'])
          for p in root.rglob('*'):
              if p.is_file() and p.suffix.lower() in {'.csv','.xlsx','.xls','.db','.sqlite'}:
                  raise AssertionError(f'Dato no permitido en payload: {p}')
          print('V4419_SCOPE_OK')
          '@ | python -

      - name: Publicar payload
        shell: pwsh
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -r --ignore-unmatch updates/v4_4_19_whatsapp_test_retention/__pycache__
          git add updates/v4_4_19_whatsapp_test_retention
          if(-not (git diff --cached --quiet)){git commit -m 'payload: v4.4.19 prueba WhatsApp completa'}
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Verificar payload remoto
        shell: pwsh
        run: python tools/remote_payload_guard.py --manifest build/v4419_whatsapp_test_retention/candidate_latest.json

      - name: Mover canal a v4.4.19
        shell: pwsh
        run: |
          Copy-Item build/v4419_whatsapp_test_retention/candidate_latest.json latest-v3.json -Force
          Copy-Item build/v4419_whatsapp_test_retention/candidate_latest.json latest.json -Force
          git add latest-v3.json latest.json
          if(-not (git diff --cached --quiet)){git commit -m 'release: publicar v4.4.19 prueba WhatsApp completa'}
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Confirmar canal final
        shell: pwsh
        run: |
          @'
          import json,time,urllib.request,pathlib,os
          url='https://raw.githubusercontent.com/fanserick-star/recepcion-dr-revelo-updates/main/latest-v3.json'
          out=pathlib.Path(os.environ['RUNNER_TEMP'])/'latest-v4419-remote.json'
          last=''
          for _ in range(45):
              try:
                  req=urllib.request.Request(url+'?rp_ts='+str(time.time_ns()),headers={'Cache-Control':'no-cache','Pragma':'no-cache','User-Agent':'RP-v4419-guard'})
                  with urllib.request.urlopen(req,timeout=10) as r: raw=r.read()
                  d=json.loads(raw.decode('utf-8-sig'))
                  if str(d.get('version'))=='4.4.19':
                      out.write_bytes(raw);print('CHANNEL_4419_VISIBLE');break
                  last=str(d.get('version'))
              except Exception as e:last=repr(e)
              time.sleep(1)
          else: raise SystemExit('Canal no propagó 4.4.19; último='+last)
          '@ | Set-Content -Encoding utf8 $env:RUNNER_TEMP\wait4419.py
          python $env:RUNNER_TEMP\wait4419.py
          python tools/remote_payload_guard.py --manifest $env:RUNNER_TEMP\latest-v4419-remote.json
          Write-Host 'V4419_PUBLISHED_AND_VERIFIED'
