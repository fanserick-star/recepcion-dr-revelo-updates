name: Construir candidata v4.3.70
on:
  push:
    branches: [main]
    paths:
      - 'build/v470/**'
      - '.github/workflows/build-v470-candidate.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Guardar huella de Agenda web
        run: git rev-parse HEAD:agenda > _agenda_tree_before.txt
      - name: Construir candidata exacta
        run: python build/v470/build_v470.py
      - name: Compilar y validar
        run: |
          cat updates/v470/app.part* > _ci_app.py
          python -m py_compile _ci_app.py
          python - <<'PY'
          import ast
          from pathlib import Path
          t=ast.parse(Path('_ci_app.py').read_text(encoding='utf-8'))
          for n in t.body:
              if isinstance(n,ast.Assign) and len(n.targets)==1 and isinstance(n.targets[0],ast.Name) and n.targets[0].id=='V460_OVERLAY_JS':
                  Path('_ci_overlay.js').write_text(ast.literal_eval(n.value),encoding='utf-8');break
          else: raise SystemExit('V460_OVERLAY_JS faltante')
          PY
          node --check _ci_overlay.js
          python build/v470/test_v470.py
      - name: Verificar que Agenda web quedó intacta
        run: |
          test "$(cat _agenda_tree_before.txt)" = "$(git rev-parse HEAD:agenda)"
          git diff --exit-code -- agenda
      - name: Publicar candidata sin mover canal
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add updates/v470
          if git diff --cached --quiet; then echo 'Sin cambios'; else git commit -m 'candidate: construir v4.3.70 verificada'; git pull --rebase origin main; git push; fi
