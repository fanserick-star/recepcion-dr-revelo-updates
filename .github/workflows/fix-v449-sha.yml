name: Verificador SHA legado (manual)

# DESACTIVADO del flujo automático desde v4.4.60.
# El release-gate es el único proceso autorizado a modificar latest-v3.json/latest.json.
# Este workflow queda únicamente para diagnóstico manual de versiones antiguas.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-info:
    runs-on: ubuntu-latest
    steps:
      - name: Estado
        run: echo "Verificador legado desactivado. La publicación automática pertenece exclusivamente a release-gate.yml."
